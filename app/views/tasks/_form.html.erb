<div class="form-group">
<%= form_with(model: [ @user, @task ]) do |field| %>

    <%= field.text_field :title, class: 'form-control', placeholder:'Título' %>
    <%= field.text_field :description, class: 'form-control', placeholder:'Descripción'  %>

    <%= field.submit %>

<% end %>
</div>